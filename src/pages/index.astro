---
const logos = [
  { file: 'alfred-avatar-definitivo.png', label: 'Logo definitivo' },
  { file: '001-logo-style-emblem-for-telegram-avatar-an.png', label: 'Propuesta OpenAI refine' },
  { file: '001-flat-vector-logo-avatar-of-lobster-profe.png', label: 'Propuesta vectorial' },
  { file: 'avatar_02_tornasol_lobster_badge.png', label: 'Variante badge' },
  { file: 'avatar_04_tornasol_lobster_split.png', label: 'Variante split' },
  { file: 'avatar_08_flat_lobster_icon.png', label: 'Variante flat icon' }
];
---
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Alfred Claw · Próximamente</title>
    <style>
      :root{--bg:#070c1d;--bg2:#101b3b;--card:#111c3f;--txt:#f2f6ff;--muted:#b6c6ea;--line:rgba(255,255,255,.12);--cta:#2c6fff;--accent:#71e8ff}
      *{box-sizing:border-box} body{margin:0;font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial;background:linear-gradient(180deg,var(--bg),var(--bg2));color:var(--txt)}
      .section{min-height:100svh;display:flex;align-items:center;justify-content:center;padding:34px 18px;border-bottom:1px solid rgba(255,255,255,.08)}
      .container{width:min(1100px,100%)}
      .hero{display:grid;grid-template-columns:220px 1fr;gap:26px;align-items:center;background:rgba(255,255,255,.04);border:1px solid var(--line);border-radius:22px;padding:30px}
      .hero img{width:220px;height:220px;border-radius:50%;border:5px solid var(--accent)}
      .pill{display:inline-block;padding:5px 10px;border:1px solid var(--line);border-radius:999px;font-size:.8rem;color:#d9e6ff;background:rgba(255,255,255,.03);margin-bottom:10px}
      h1{margin:0;font-size:clamp(2.2rem,6vw,4.6rem);line-height:1.02;letter-spacing:-.03em}
      .sub{margin:12px 0 0;font-size:clamp(1.08rem,2.4vw,1.5rem);max-width:20ch;color:#dbe7ff}
      .cta-row{margin-top:16px;display:flex;gap:10px;flex-wrap:wrap;align-items:center}
      .cta{display:inline-block;background:var(--cta);color:#fff;text-decoration:none;font-weight:700;padding:13px 18px;border-radius:12px;border:1px solid #4e86ff}
      .ghost{color:#cfe1ff;font-size:.95rem}

      .counter-wrap{display:flex;justify-content:center;margin-top:18px}
      .counter{display:flex;align-items:center;gap:8px;padding:8px 12px;background:rgba(255,255,255,.04);border:1px solid var(--line);border-radius:999px}
      .counter-num{font-weight:700}

      .center{text-align:center}
      h2{margin:0;font-size:clamp(2rem,5vw,4rem);line-height:1.03;letter-spacing:-.02em}
      .subtitle{margin:12px auto 0;max-width:22ch;color:var(--muted);font-size:clamp(1rem,2vw,1.25rem)}
      .grid{margin-top:22px;display:grid;grid-template-columns:repeat(auto-fit,minmax(230px,1fr));gap:14px}
      .card{padding:22px;background:rgba(255,255,255,.04);border:1px solid var(--line);border-radius:16px}
      .ico{display:block;font-size:1.45rem;margin-bottom:8px}
      .card b{font-size:1.2rem}
      .card p{margin:7px 0 0;color:#c8d8ff}

      .wait{max-width:760px;margin:22px auto 0;padding:20px;background:rgba(255,255,255,.04);border:1px solid var(--line);border-radius:16px}
      .row{display:flex;gap:10px;flex-wrap:wrap}
      input[type=email]{flex:1;min-width:230px;background:#0c1733;color:var(--txt);border:1px solid var(--line);padding:14px;border-radius:12px;font-size:1rem}
      button{background:var(--cta);color:#fff;border:1px solid #4e86ff;padding:14px 16px;border-radius:12px;font-weight:700;font-size:1rem;cursor:pointer;white-space:nowrap}
      .ok{display:none;margin-top:10px;color:#b9ffd8;font-size:.95rem}

      .logos{margin-top:22px;display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:14px}
      .logo-card{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:10px}
      .logo-card img{width:100%;aspect-ratio:1/1;object-fit:cover;border-radius:10px}
      .label{margin-top:7px;color:#d4e0ff;font-size:.93rem}

      @media (max-width:760px){
        .hero{grid-template-columns:1fr;text-align:center}
        .hero img{margin:0 auto;width:180px;height:180px}
        .cta-row{justify-content:center}
        .row{flex-direction:column}
        input[type=email],button{width:100%}
      }
    </style>
  </head>
  <body>
    <section class="section">
      <div class="container">
        <div class="hero">
          <img src="/alfred-landing/logos/alfred-avatar-definitivo.png" alt="Logo de Alfred" />
          <div>
            <span class="pill">🦞 OpenClaw</span>
            <h1>ALFRED CLAW</h1>
            <p class="sub">Automatiza tareas. Ahorra tiempo. Mantén el control.</p>
            <div class="cta-row">
              <a href="#waitlist" class="cta">Quiero acceso temprano</a>
              <span class="ghost">Disponible próximamente</span>
            </div>
            <div class="counter-wrap">
              <div class="counter"><span>👀</span><span class="counter-num" id="visit-count">…</span></div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="container center">
        <h2>RÁPIDO. ÚTIL. ADAPTABLE.</h2>
        <p class="subtitle">Sin humo. Resultados prácticos desde el primer día.</p>
        <div class="grid">
          <article class="card"><span class="ico">⚡</span><b>Rápido</b><p>Respuestas directas al momento.</p></article>
          <article class="card"><span class="ico">🧠</span><b>Útil</b><p>Acciones reales que te ahorran tiempo.</p></article>
          <article class="card"><span class="ico">🎯</span><b>Adaptable</b><p>Se ajusta a tu forma de trabajar.</p></article>
        </div>
      </div>
    </section>

    <section class="section" id="waitlist">
      <div class="container center">
        <h2>ÚNETE A LA LISTA</h2>
        <p class="subtitle">Déjame tu correo y te aviso cuando abramos.</p>
        <div class="wait">
          <form id="waitlist-form">
            <div class="row">
              <input id="email" type="email" placeholder="tu@email.com" required />
              <button type="submit">Apuntarme</button>
            </div>
            <div class="ok" id="ok-msg">¡Perfecto! Te avisaré pronto ✅</div>
          </form>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="container center">
        <h2>EVOLUCIÓN DEL LOGO</h2>
        <p class="subtitle">Versiones en las que hemos trabajado.</p>
        <div class="logos">
          {logos.map((logo) => (
            <article class="logo-card">
              <img src={`/alfred-landing/logos/${logo.file}`} alt={logo.label} loading="lazy" />
              <div class="label">{logo.label}</div>
            </article>
          ))}
        </div>
      </div>
    </section>

    <script>
      (async () => {
        const countEl = document.getElementById('visit-count');
        const localVisits = () => {
          const k='alfred_visits_fallback';
          const n=(parseInt(localStorage.getItem(k)||'0',10)||0)+1;
          localStorage.setItem(k,String(n));
          return n;
        };
        try {
          const r = await fetch('https://api.countapi.xyz/hit/alfred-claw-artiles/alfred-landing-visits');
          const d = await r.json();
          countEl.textContent = Number.isFinite(d?.value) ? String(d.value) : String(localVisits());
        } catch { countEl.textContent = String(localVisits()); }

        const form = document.getElementById('waitlist-form');
        const ok = document.getElementById('ok-msg');
        form.addEventListener('submit', (e) => {
          e.preventDefault();
          const email = (document.getElementById('email').value || '').trim().toLowerCase();
          if (!email) return;
          const key='alfred_waitlist_txt';
          const prev=localStorage.getItem(key)||'';
          const line=`${new Date().toISOString()}\t${email}`;
          localStorage.setItem(key,(prev?prev+'\n':'')+line);
          ok.style.display='block';
          form.reset();
        });
      })();
    </script>
  </body>
</html>
