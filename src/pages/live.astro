---
const logos = [
  { file: 'alfred-avatar-definitivo.png', label: 'Logo definitivo' },
  { file: '001-logo-style-emblem-for-telegram-avatar-an.png', label: 'Propuesta OpenAI refine' },
  { file: '001-flat-vector-logo-avatar-of-lobster-profe.png', label: 'Propuesta vectorial' },
  { file: 'avatar_02_tornasol_lobster_badge.png', label: 'Variante badge' },
  { file: 'avatar_04_tornasol_lobster_split.png', label: 'Variante split' },
  { file: 'avatar_08_flat_lobster_icon.png', label: 'Variante flat icon' }
];
---
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Alfred Claw · Acceso anticipado</title>
    <style>
      :root { --bg:#070c1d; --bg2:#101b3b; --card:rgba(255,255,255,.05); --txt:#f2f6ff; --muted:#b6c6ea; --line:rgba(255,255,255,.14); --cta:#2c6fff; --cta2:#2458cc; --accent:#71e8ff; }
      * { box-sizing: border-box; }
      html,body { margin:0; }
      body { font-family: Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif; color:var(--txt); background:linear-gradient(180deg,var(--bg),var(--bg2)); }
      .section { min-height:100svh; display:flex; align-items:center; justify-content:center; padding:34px 16px; border-bottom:1px solid rgba(255,255,255,.08); }
      .container { width:min(1080px,100%); }

      .hero { display:grid; grid-template-columns:260px 1fr; gap:28px; align-items:center; background:var(--card); border:1px solid var(--line); border-radius:20px; padding:28px; }
      .hero-logo { width:260px; height:260px; border-radius:50%; border:6px solid var(--accent); object-fit:cover; }
      .badge { display:inline-flex; align-items:center; gap:6px; padding:5px 10px; border:1px solid var(--line); border-radius:999px; font-size:.8rem; color:#dbe7ff; letter-spacing:.01em; }
      h1 { margin:10px 0 8px; font-size:clamp(2.2rem,6vw,4.6rem); line-height:.96; letter-spacing:-.03em; }
      .lead { margin:0; max-width:28ch; color:#dce7ff; font-size:clamp(1rem,2vw,1.22rem); line-height:1.35; }
      .hero-row { margin-top:14px; display:flex; flex-wrap:wrap; gap:10px; align-items:center; }
      .cta { text-decoration:none; color:#fff; background:linear-gradient(180deg,#3a74ff,#2a5fe0); border:1px solid #6991ff; padding:12px 16px; border-radius:10px; font-weight:700; font-size:.98rem; box-shadow:0 6px 18px rgba(44,111,255,.28); }
      .soon { color:var(--muted); font-size:.94rem; }
      .counter-wrap { margin-top:12px; display:flex; justify-content:center; width:100%; }
      .counter { display:inline-flex; align-items:center; gap:8px; border:1px solid var(--line); border-radius:999px; padding:7px 12px; background:rgba(255,255,255,.04); margin:0 auto; }
      .counter-num { font-weight:700; }

      .center { text-align:center; }
      h2 { margin:0; font-size:clamp(2rem,5vw,3.7rem); line-height:1.02; letter-spacing:-.02em; }
      .sub { margin:10px auto 0; max-width:28ch; color:var(--muted); font-size:clamp(1rem,2vw,1.2rem); }
      .grid { margin-top:18px; display:grid; grid-template-columns:repeat(3,minmax(0,1fr)); gap:12px; }
      .tile { text-align:center; background:var(--card); border:1px solid var(--line); border-radius:14px; padding:18px; }
      .ico { font-size:1.35rem; display:block; margin-bottom:6px; }
      .tile b { font-size:1.1rem; }
      .tile p { margin:6px 0 0; color:var(--muted); }

      .wait { margin:18px auto 0; max-width:760px; background:var(--card); border:1px solid var(--line); border-radius:14px; padding:14px; }
      .row { display:flex; gap:10px; }
      input[type=email] { flex:1; min-width:200px; border-radius:12px; border:1px solid var(--line); background:rgba(11,23,53,.9); color:var(--txt); padding:14px; font-size:1rem; }
      button { border-radius:12px; border:1px solid #4a7dff; background:linear-gradient(180deg,var(--cta),var(--cta2)); color:#fff; font-weight:700; padding:14px 16px; white-space:nowrap; cursor:pointer; }
      .ok { display:none; margin-top:10px; color:#b9ffd8; font-size:.92rem; }

      .logos { margin-top:18px; display:grid; grid-template-columns:repeat(auto-fit,minmax(200px,1fr)); gap:12px; }
      .logo-card { background:var(--card); border:1px solid var(--line); border-radius:14px; padding:8px; }
      .logo-card img { width:100%; aspect-ratio:1/1; object-fit:cover; border-radius:10px; }
      .label { margin-top:6px; font-size:.9rem; color:var(--muted); }

      @media (max-width:920px) {
        .hero { grid-template-columns:1fr; text-align:center; }
        .hero-logo { width:200px; height:200px; margin:0 auto; }
        .lead { margin-inline:auto; }
        .hero-row,.counter-wrap { justify-content:center; }
        .grid { grid-template-columns:1fr; }
      }
      @media (max-width:620px) {
        .row { flex-direction:column; }
        input[type=email],button { width:100%; }
      }
    </style>
  </head>
  <body>
    <section class="section">
      <div class="container">
        <div class="hero">
          <img class="hero-logo" src="/alfred-landing/logos/alfred-avatar-definitivo.png" alt="Logo de Alfred" />
          <div>
            <span class="badge">🦞 by OpenClaw</span>
            <h1>ALFRED CLAW</h1>
            <p class="lead">Automatiza tareas y ejecuta mejor tu día con un asistente realmente útil.</p>
            <div class="hero-row">
              <a class="cta" href="#waitlist">Unirme a la lista</a>
            </div>
            <div class="counter-wrap">
              <div class="counter"><span>👀</span><span class="counter-num" id="visit-count">…</span></div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="container center">
        <h2>Rápido. Útil. Adaptable.</h2>
        <p class="sub">Menos ruido, más resultados.</p>
        <div class="grid">
          <article class="tile"><span class="ico">⚡</span><b>Rápido</b><p>Respuestas claras al instante.</p></article>
          <article class="tile"><span class="ico">🧠</span><b>Útil</b><p>Automatización real para tu día.</p></article>
          <article class="tile"><span class="ico">🎯</span><b>Adaptable</b><p>Se ajusta a tu forma de trabajar.</p></article>
        </div>
      </div>
    </section>

    <section class="section" id="waitlist">
      <div class="container center">
        <h2>Únete a la lista</h2>
        <p class="sub">Te avisaré cuando abramos acceso.</p>
        <div class="wait">
          <form id="waitlist-form">
            <div class="row">
              <input id="email" type="email" required placeholder="tu@email.com" />
              <button type="submit">Apuntarme</button>
            </div>
            <div class="ok" id="ok-msg">¡Perfecto! Quedas dentro ✅</div>
          </form>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="container center">
        <h2>Evolución del logo</h2>
        <p class="sub">Del concepto al definitivo.</p>
        <div class="logos">
          {logos.map((logo) => (
            <article class="logo-card">
              <img src={`/alfred-landing/logos/${logo.file}`} alt={logo.label} loading="lazy" />
              <div class="label">{logo.label}</div>
            </article>
          ))}
        </div>
      </div>
    </section>

    <script>
      (async () => {
        const countEl = document.getElementById('visit-count');
        const localVisits = () => {
          const k = 'alfred_visits_fallback';
          const n = (parseInt(localStorage.getItem(k) || '0', 10) || 0) + 1;
          localStorage.setItem(k, String(n));
          return n;
        };
        try {
          const r = await fetch('https://api.countapi.xyz/hit/alfred-claw-artiles/alfred-landing-visits');
          const d = await r.json();
          countEl.textContent = Number.isFinite(d?.value) ? String(d.value) : String(localVisits());
        } catch { countEl.textContent = String(localVisits()); }

        const form = document.getElementById('waitlist-form');
        const ok = document.getElementById('ok-msg');
        form.addEventListener('submit', (e) => {
          e.preventDefault();
          const email = (document.getElementById('email').value || '').trim().toLowerCase();
          if (!email) return;
          const key = 'alfred_waitlist_txt';
          const prev = localStorage.getItem(key) || '';
          const line = `${new Date().toISOString()}\t${email}`;
          localStorage.setItem(key, (prev ? prev + '\n' : '') + line);
          ok.style.display = 'block';
          form.reset();
        });
      })();
    </script>
  </body>
</html>
