---
const logos = [
  { file: 'alfred-avatar-definitivo.png', label: 'Logo definitivo' },
  { file: '001-logo-style-emblem-for-telegram-avatar-an.png', label: 'Propuesta OpenAI refine' },
  { file: '001-flat-vector-logo-avatar-of-lobster-profe.png', label: 'Propuesta vectorial' },
  { file: 'avatar_02_tornasol_lobster_badge.png', label: 'Variante badge' },
  { file: 'avatar_04_tornasol_lobster_split.png', label: 'Variante split' },
  { file: 'avatar_08_flat_lobster_icon.png', label: 'Variante flat icon' }
];
---
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Alfred Claw · Future Edition</title>
    <meta name="description" content="Versión alternativa Future de Alfred Claw." />
    <link rel="canonical" href="https://alfred-claw-artiles.github.io/alfred-landing/" />
    <meta name="robots" content="noindex,follow" />
    <style>
      :root{--bg:#031023;--bg2:#08233f;--line:rgba(255,255,255,.16);--txt:#f6fbff;--muted:#bbd4ec;--gold:#f8c75c;--cyan:#67e7ff}
      *{box-sizing:border-box} html,body{margin:0}
      body{font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial;background:radial-gradient(1200px 680px at 75% 10%,rgba(103,231,255,.2),transparent 60%),linear-gradient(180deg,var(--bg),var(--bg2));color:var(--txt)}
      .wrap{max-width:1180px;margin:0 auto;padding:18px}
      .hero{min-height:100svh;display:grid;grid-template-columns:1fr .85fr;gap:18px;align-items:stretch}
      .hero-a{border:1px solid var(--line);border-radius:18px;padding:24px;background:rgba(255,255,255,.04);display:flex;flex-direction:column;justify-content:center}
      .tag{display:inline-flex;align-items:center;gap:8px;padding:6px 10px;border:1px solid var(--line);border-radius:999px;font-size:.82rem;color:#dbebff;width:max-content}
      h1{margin:14px 0 10px;font-size:clamp(2.4rem,8vw,6.2rem);line-height:.9;letter-spacing:-.04em;text-transform:uppercase}
      .lead{margin:0;max-width:28ch;font-size:clamp(1rem,2vw,1.2rem);color:var(--muted)}
      .row{display:flex;gap:10px;flex-wrap:wrap;align-items:center;margin-top:16px}
      .cta{background:linear-gradient(180deg,#ffe08b,var(--gold));color:#121212;border:1px solid #ffe7a8;text-decoration:none;padding:12px 16px;border-radius:10px;font-weight:800}
      .mini{color:#d9e9fb;font-size:.92rem}
      .count{margin-top:16px;display:inline-flex;gap:8px;align-items:center;border:1px solid var(--line);border-radius:999px;padding:8px 12px;background:rgba(255,255,255,.06)}
      .hero-b{border:1px solid var(--line);border-radius:18px;background:linear-gradient(180deg,rgba(255,255,255,.06),rgba(255,255,255,.02));display:grid;place-items:center;position:relative;overflow:hidden}
      .hero-b:before{content:"";position:absolute;inset:10% 8%;border:1px solid rgba(255,255,255,.15);border-radius:16px}
      .hero-b img{width:min(76%,420px);border-radius:50%;border:6px solid var(--cyan);position:relative}

      section{padding:90px 0;border-top:1px solid rgba(255,255,255,.1)}
      h2{margin:0;font-size:clamp(2rem,5vw,4rem);line-height:.96;text-transform:uppercase;letter-spacing:-.03em}
      .sub{margin:10px 0 0;color:var(--muted);font-size:1.05rem}
      .cards{margin-top:20px;display:grid;grid-template-columns:repeat(3,1fr);gap:12px}
      .card{padding:18px;background:rgba(255,255,255,.04);border:1px solid var(--line);border-radius:12px;text-align:center}
      .card .ico{font-size:1.3rem;display:block;margin-bottom:6px}
      .card p{margin:7px 0 0;color:#d3e4f6}

      .wait{margin-top:20px;max-width:760px;background:rgba(255,255,255,.04);border:1px solid var(--line);border-radius:12px;padding:12px}
      .row2{display:flex;gap:10px}
      input[type=email]{flex:1;min-width:210px;background:#0b1e36;color:var(--txt);border:1px solid var(--line);border-radius:10px;padding:13px;font-size:1rem}
      button{background:linear-gradient(180deg,#ffe08b,var(--gold));color:#111;border:1px solid #ffe7a8;border-radius:10px;padding:13px 16px;font-weight:800;cursor:pointer}
      .ok{display:none;margin-top:8px;color:#bfffd0}

      .logos{margin-top:20px;display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:12px}
      .logo{padding:8px;background:rgba(255,255,255,.04);border:1px solid var(--line);border-radius:12px}
      .logo img{width:100%;aspect-ratio:1/1;object-fit:cover;border-radius:8px}
      .lbl{margin-top:6px;color:#c4d9ee;font-size:.9rem}

      @media (max-width:920px){.hero{grid-template-columns:1fr;min-height:auto}.cards{grid-template-columns:1fr}}
      @media (max-width:620px){.row2{flex-direction:column}input[type=email],button{width:100%}}
    </style>
  </head>
  <body>
    <main class="wrap">
      <header class="hero">
        <div class="hero-a">
          <span class="tag">🦞 by OpenClaw</span>
          <h1>Alfred<br/>Claw</h1>
          <p class="lead">Automatiza tareas y ejecuta mejor tu día con un asistente realmente útil.</p>
          <div class="row">
            <a class="cta" href="#waitlist">Unirme a la lista</a>
            <span class="mini">Acceso anticipado</span>
          </div>
          <div class="count"><span>👀</span><span id="visit-count">…</span></div>
        </div>
        <div class="hero-b"><img src="/alfred-landing/logos/alfred-avatar-definitivo.png" alt="Logo de Alfred" /></div>
      </header>

      <section>
        <h2>Rápido · Útil · Adaptable</h2>
        <p class="sub">Menos ruido, más resultados.</p>
        <div class="cards">
          <article class="card"><span class="ico">⚡</span><b>Rápido</b><p>Respuestas claras al instante.</p></article>
          <article class="card"><span class="ico">🧠</span><b>Útil</b><p>Automatización real para tu día.</p></article>
          <article class="card"><span class="ico">🎯</span><b>Adaptable</b><p>Se ajusta a tu forma de trabajar.</p></article>
        </div>
      </section>

      <section id="waitlist">
        <h2>Únete a la lista</h2>
        <p class="sub">Te avisaré cuando abramos acceso.</p>
        <div class="wait">
          <form id="waitlist-form">
            <div class="row2">
              <input id="email" type="email" required placeholder="tu@email.com" />
              <button type="submit">Apuntarme</button>
            </div>
            <div class="ok" id="ok-msg">¡Perfecto! Quedas dentro ✅</div>
          </form>
        </div>
      </section>

      <section>
        <h2>Evolución del logo</h2>
        <p class="sub">Del concepto al definitivo.</p>
        <div class="logos">
          {logos.map((logo) => (
            <article class="logo">
              <img src={`/alfred-landing/logos/${logo.file}`} alt={logo.label} loading="lazy" />
              <div class="lbl">{logo.label}</div>
            </article>
          ))}
        </div>
      </section>
    </main>

    <script>
      (async () => {
        const countEl = document.getElementById('visit-count');
        const cacheKey = 'alfred_visits_global_cache';
        const setCount = (v) => {
          if (Number.isFinite(v)) {
            countEl.textContent = String(v);
            localStorage.setItem(cacheKey, String(v));
            return true;
          }
          return false;
        };
        try {
          const r1 = await fetch('https://api.counterapi.dev/v1/alfred-claw-artiles/landing-main/up');
          const d1 = await r1.json();
          if (!setCount(d1?.count)) throw new Error('bad counterapi value');
        } catch {
          countEl.textContent = localStorage.getItem(cacheKey) || '—';
        }

        const form = document.getElementById('waitlist-form');
        const ok = document.getElementById('ok-msg');
        form.addEventListener('submit', (e) => {
          e.preventDefault();
          const email = (document.getElementById('email').value || '').trim().toLowerCase();
          if (!email) return;
          const key = 'alfred_waitlist_txt';
          const prev = localStorage.getItem(key) || '';
          const line = `${new Date().toISOString()}\t${email}`;
          localStorage.setItem(key, (prev ? prev + '\n' : '') + line);
          ok.style.display = 'block';
          form.reset();
        });
      })();
    </script>
  </body>
</html>
